<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="APP_REQUERIMIENTOS.MVVM.Vistas.PedidoProductoView"
              x:Name="thispage"
              xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             Title="Pedidos Productos">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button" Class="Activo">
                <Setter Property="BackgroundColor" Value="Green"/>
                <Setter Property="TextColor" Value="White"/>
            </Style>

            <Style TargetType="Button" Class="Inactivo">
                <Setter Property="BackgroundColor" Value="Red"/>
                <Setter Property="TextColor" Value="White"/>
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <CollectionView ItemsSource="{Binding listaproducto}"
            SelectionMode="None" ItemSizingStrategy="MeasureFirstItem"
            ItemsLayout="VerticalGrid,2"
            VerticalScrollBarVisibility="Never" Grid.Column="0" Grid.Row="0">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border 
                    StrokeShape="RoundRectangle 30"
                    BackgroundColor="White"
                    Padding="2,2"
                    Margin="5,5"
                    HeightRequest="250" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="120" />
                                <RowDefinition Height="50" />
                                <RowDefinition Height="40" />
                                <RowDefinition Height="30" />
                            </Grid.RowDefinitions>
                            <Image Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Source="{Binding rutaarchivo}"
                         Aspect="AspectFill"  WidthRequest="150" HeightRequest="120" HorizontalOptions="CenterAndExpand" />


                            <Label Text="{Binding Nombre}" Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
                          FontAttributes="Bold" FontSize="16"/>

                            <Label x:Name="lblCantidad" Grid.Column="0" Grid.Row="2"
                          Text="{ Binding Cantidad }" 
                          FontSize="18"
                          VerticalOptions="Center"  HorizontalTextAlignment="Center"/>
                            <Stepper Minimum="0" Grid.Column="1" Grid.Row="2"
                            Maximum="100"
                            Increment="1"
                            Value="{ Binding Cantidad}">
                                <Stepper.Behaviors>
                                    <toolkit:EventToCommandBehavior
                                EventName="ValueChanged"
                                Command="{Binding Path=BindingContext.Sllepercommand,Source={x:Reference thispage}}"
                                CommandParameter="{Binding .}" />
                                </Stepper.Behaviors>

                            </Stepper>


                            <Button Text="{Binding NombreButon}" Grid.Column="0" Padding="10,2"  Grid.Row="3" Grid.ColumnSpan="2" 
                                    BackgroundColor="{Binding Colorfondo}"
                                    HorizontalOptions="Center"    Command="{Binding Path=BindingContext.Agregarcommand,Source={x:Reference thispage}}"
                                    CommandParameter="{Binding .}"></Button>


                        </Grid>


                    </Border>


                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <StackLayout Orientation="Horizontal" Grid.Column="0" Grid.Row="1" BackgroundColor="Blue" Spacing="10" Padding="20,5">
            <Label  HorizontalOptions="StartAndExpand" Text="VER PEDIDO " FontSize="20" HeightRequest="50" TextColor="White" ></Label>

            <Border   StrokeShape="RoundRectangle 30" BackgroundColor="White" HorizontalOptions="FillAndExpand">
                <StackLayout Orientation="Horizontal" Padding="15,2">
                    <Label  Text="{Binding importetotal, StringFormat='S/{0:F2}'}" FontSize="20" HeightRequest="50" VerticalTextAlignment="Center" TextColor="Black"   HorizontalOptions="FillAndExpand" ></Label>
                    <Image Source="realizarpedido"  WidthRequest="40" HeightRequest="40" Aspect="AspectFill"   />

                </StackLayout>

            </Border>

            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding IraPedidoscommand}"/>
            </StackLayout.GestureRecognizers>

        </StackLayout>

        <Grid  Grid.Column="0" Grid.Row="0"  Grid.RowSpan="2" Opacity=".8" 
                IsVisible="{Binding flgindicador}" InputTransparent="False"  BackgroundColor="#FAFAFA">

            <Grid.GestureRecognizers>
                <TapGestureRecognizer />
            </Grid.GestureRecognizers>

            <ActivityIndicator
             HorizontalOptions="Center"
             IsRunning="{Binding flgindicador}"
             Color="#DF013A"
             />
        </Grid>

    </Grid>
</ContentPage>