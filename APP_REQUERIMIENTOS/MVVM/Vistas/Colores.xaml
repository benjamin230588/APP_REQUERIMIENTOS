<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="APP_REQUERIMIENTOS.MVVM.Vistas.Colores"
             Title="Colores">


    <ContentPage.Resources>
        <Color x:Key="Primary">#ab3527</Color>
        <Color x:Key="Secondary">#775752</Color>
        <Color x:Key="Tertiary">#705c2e</Color>
    </ContentPage.Resources>


    <Grid x:Name="CONTENEDOR"   BackgroundColor="Blue">
        <Frame Margin="10,0,10,0" VerticalOptions="Center" HeightRequest="500">
            <StackLayout Orientation="Vertical" Spacing="20">

                
                <Label
                    FontAttributes="Bold"
                    FontSize="Large"
                    HorizontalTextAlignment="Center"
                    Text="Color Maker" />
                <Label Text="Red Value:" />
                <Slider
                    x:Name="sldRed"
                    MaximumTrackColor="{StaticResource Tertiary}"
                    MinimumTrackColor="{StaticResource Secondary}"
                    ThumbColor="{StaticResource Primary}"
                    ValueChanged="sldRed_ValueChanged" />
                <Label Text="Green Value:" />
                <Slider
                    x:Name="sldGreen"
                    MaximumTrackColor="{StaticResource Tertiary}"
                    MinimumTrackColor="{StaticResource Secondary}"
                    ThumbColor="{StaticResource Primary}"
                    ValueChanged="sldRed_ValueChanged" />
                <Label Text="Blue Value:" />
                <Slider
                    x:Name="sldBlue"
                    MaximumTrackColor="{StaticResource Tertiary}"
                    MinimumTrackColor="{StaticResource Secondary}"
                    ThumbColor="{StaticResource Primary}"
                    ValueChanged="sldRed_ValueChanged" />
                <Frame BackgroundColor="Aquamarine" HorizontalOptions="Center">
                    <StackLayout Orientation="Horizontal">
                        <Label
                             x:Name="lblHex"
                            HorizontalTextAlignment="End"
                            Text="HEX Value: #000000"
                            VerticalOptions="Center" >
                            
                        </Label>

                        <ImageButton
                            Margin="10,0,0,0"
                            HeightRequest="25"
                            Source="copy.svg"
                            VerticalOptions="Center"
                            WidthRequest="25"
                            Clicked="ImageButton_Clicked">
                            
                        </ImageButton>
                    </StackLayout>
                    
                </Frame>
            </StackLayout>
            
            
        </Frame>

    </Grid>
</ContentPage>