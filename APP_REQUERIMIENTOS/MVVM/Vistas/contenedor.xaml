<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="APP_REQUERIMIENTOS.MVVM.Vistas.contenedor"
             Title="contenedor">
    <Grid RowDefinitions="*">
        <CollectionView ItemsSource="{Binding Categorias}"
                    
                SelectionChanged="CollectionView_SelectionChanged" 
                Margin="10"
                ItemSizingStrategy="MeasureFirstItem"
                ItemsLayout="VerticalGrid,2"
                VerticalScrollBarVisibility="Never"
               >
            

            <CollectionView.ItemTemplate>
                <DataTemplate>
                  
                        <Border 
                                StrokeShape="RoundRectangle 30"
                                BackgroundColor="White"
                                Padding="10"
                                Margin="6"
                                HeightRequest="200" InputTransparent="True">

                        

                        <StackLayout Orientation="Vertical" Spacing="10" HorizontalOptions="Center">
                            <Image  Source="https://www.gatitasmegaplaza.pe/wp-content/uploads/2024/08/photo_2024-08-19_23-03-56-5.jpg"
                                 HeightRequest="90"
                                 WidthRequest="90"
                                 Aspect="AspectFit"/>
                                           
                            <Label Text="{Binding Nombre}"
                                        FontSize="Medium"
                                        HorizontalTextAlignment="Center"/>
                                </StackLayout>

                    </Border>
                      

                  
                   
                    
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>